# The manifest for the "mapout-authentication" service.
# Read the full specification for the "Load Balanced Web Service" type at:
#  https://aws.github.io/copilot-cli/docs/manifest/lb-web-service/

# Your service name will be used in naming your resources like log groups, ECS services, etc.
name: mapout-authentication
type: Load Balanced Web Service

# Distribute traffic to your service.
http:
  # Requests to this path will be forwarded to your service.
  # To match all requests you can use the "/" path.
  path: 'mapout-authentication'
  # You can specify a custom health check path. The default is "/".
  healthcheck: '/mapout-authentication'
  alias: ${COPILOT_ENVIRONMENT_NAME}.api-gateway.mapout.com

# Configuration for your containers and service.
image:
  # Docker build arguments. For additional overrides: https://aws.github.io/copilot-cli/docs/manifest/lb-web-service/#image-build
  build: .Dockerfile
  # Port exposed through your container to route traffic to it.
  port: 9000

cpu: 256       # Number of CPU units for the task.
memory: 512    # Amount of memory in MiB used by the task.
platform: linux/x86_64  # See https://aws.github.io/copilot-cli/docs/manifest/lb-web-service/#platform
count: 1       # Number of tasks that should be running in your service.
exec: true     # Enable running commands in your container.
network:
  connect: true # Enable Service Connect for intra-environment traffic between services.

# storage:
  # readonly_fs: true       # Limit to read-only access to mounted root filesystems.
 
# Optional fields for more advanced use-cases.
#
variables:                    # Pass environment variables as key value pairs.
 MSG91_AUTHKEY: "323087AFY8kCGB63aad362P1"
 MSG91_OTP_TEMPLATE_ID: "63d0fb32d6fc050f200f3283"
 APIKEY:  "b6389f99-f875-473a-93f7-a873a6f9ebea"
 COPILOT_ENVIRONMENT_NAME: "dev"
 COPILOT_SERVICE_DISCOVERY_ENDPOINT: "dev.api-gateway.mapout.com"
 MAPOUT_GOOGLE_CLIENT: "317080843821-ci3981j0eji21mvja399lqvpe364t42a.apps.googleusercontent.com"
 HRGIG_GOOGLE_CLIENT: "419494917881-r9g5skapv0lo7sh2vttu56fnc9o1trlk.apps.googleusercontent.com"
 GOOGLE_WEB_CLIENT_ID: "8427177136-3m9i4fekic8hq4raabo682l23dm0d6ir.apps.googleusercontent.com"
 GOOGLE_IOS_CLIENT_ID: "8427177136-3m9i4fekic8hq4raabo682l23dm0d6ir.apps.googleusercontent.com"
 GOOGLE_ANDROID_CLIENT_ID: "8427177136-3m9i4fekic8hq4raabo682l23dm0d6ir.apps.googleusercontent.com"
 PAYMENTS_SERVICE_ENTRY_POINT: "https://dev.api-gateway.mapout.com/mapout-payments/api"
 NOTIFICATIONS_SERVICE_ENTRY_POINT: "https://dev.api-gateway.mapout.com/notify-node/api/notify"
 MAPOUT_LINKEDIN_CLIENT: "77lyg466k2tfzs"
 MAPOUT_LINKEDIN_SECRET : "Kh018UwJZ6autJOU"
 HRGIG_LINKEDIN_CLIENT: "77jdxl9y1amai1"
 HRGIG_LINKEDIN_SECRET: "xhTriIHQW3IGohql"

#secrets:                      # Pass secrets from AWS Systems Manager (SSM) Parameter Store.
#  GITHUB_TOKEN: GITHUB_TOKEN  # The key is the name of the environment variable, the value is the name of the SSM parameter.

# You can override any of the values defined above by environment.
environments:
  dev:
    count: 1               # Number of tasks to run for the "test" environment.
    variables:
      # Move MongoDB key to secrets in the nearest future
      MAPOUT_MONGODB_URI: "mongodb+srv://mapout:mapout@mapoutdb.hj2on.mongodb.net/mapout-staging"
      HRGIG_MONGODB_URI: "mongodb+srv://mapout:mapout@mapoutdb.hj2on.mongodb.net/hrgig-staging"
      NODE_ENV: "dev"
  production:
    range: 2-10
    variables:
      # Move MongoDB key to secrets in the nearest future
      MAPOUT_MONGODB_URI: "mongodb+srv://mapout:mapout@mapoutdb.hj2on.mongodb.net/mapout-production"
      HRGIG_MONGODB_URI: "mongodb+srv://mapout:mapout@mapoutdb.hj2on.mongodb.net/hrgig-production"
      NODE_ENV: "production"
      USER_COUNT_CAP: 1000
#    deployment:            # The deployment strategy for the "test" environment.
#       rolling: 'recreate' # Stops existing tasks before new ones are started for faster deployments.